// Example prototype: proves the #/proto/<name> convention. Delete once a real prototype exists.
import { PlaceholderScene } from "../../PlaceholderScene";

export default function SpinningCubePrototype() {
  return <PlaceholderScene />;
}
